<UserControl x:Class="MvvmNavigationApp.Views.AccountView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid HorizontalAlignment="Center" VerticalAlignment="Center">
            <Label Content="Enter the Text" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="325,128,325,0" Width="150"/>
            <TextBox Tag="Enter UserName" Text="{Binding UserName}" Height="50" Width="150" Margin="325,187,325,212.6" TextChanged="TextBox_TextChanged" />
            <TextBox Tag="Enter Password" Text="{Binding Password}" Height="50" Width="150" Margin="325,244,325,155.6" />
        </Grid>
        <Button Content="Home" Command="{Binding GoToHomeView}" VerticalAlignment="Bottom" HorizontalAlignment="Center"/>
    </Grid>
</UserControl>
